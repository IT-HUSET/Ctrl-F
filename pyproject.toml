[project]
name = "ctrl-f"
version = "0.1.0"
description = "Natural-language questions over If Industrial policy documents, with citations"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pymupdf>=1.24",
    "pillow>=10.0",
    "pytesseract>=0.3.10",
    "streamlit>=1.40",
    "requests>=2.32",
    "rapidocr-onnxruntime>=1.4.4",
]

[tool.uv]
package = false
